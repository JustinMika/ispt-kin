$(document).ready(function() {
    $("#form_transaction").submit(function(e) {
        e.preventDefault();
        update_poste_depense();
    });

    // function _. soumettre et enregistrer les donnees dans le formulaire
    function update_poste_depense() {

    }
});